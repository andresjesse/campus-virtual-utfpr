import type {BlockEditorProps} from "@/components/content-input/content-blocks/overlay/registry.ts";

export default function ImageBlockEditor({ content, onChange }: BlockEditorProps) {
  return (
    <div>
      <p>images!</p>
    </div>
  );
}